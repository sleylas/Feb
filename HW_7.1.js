let postalLocker = [null, null, null, "4 - ваша посылка", null];
let cell = 4;
let balance = 100;
let smsCode = "A001DFX0";

if (postalLocker[cell - 1] && smsCode === "A001DFX0") {
    console.log(`Вы получили посылку из ячейки ${cell}, и ваш счет составляет:... p`);
}